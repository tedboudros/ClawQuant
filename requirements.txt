# Core dependencies (5 packages)
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0
aiohttp>=3.9
httpx>=0.27

# CLI (setup wizard, interactive config)
questionary>=2.0

# Plugin dependencies (install only what you use)
# python-telegram-bot>=21.0    # Telegram integration
# yfinance>=0.2                # Yahoo Finance market data (optional, not used by default)
# selenium>=4.20.0             # Browser automation plugin
# Finnhub market data uses httpx (already a core dep) -- no extra package needed.
# Add FINNHUB_API_KEY to ~/.clawquant/.env to enable.
